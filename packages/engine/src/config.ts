/* ============================================================
   CONFIG — усі цифри гри в одному місці.
   Один і той самий файл для сервера, клієнта і симуляцій.

   ВАЖЛИВО: фізика рахується в КЛІТИНКАХ, не в пікселях
   (1 одиниця = 1 блок). Тому гра однаково поводиться
   на будь-якому розмірі екрана.

   Так само важливо: крок симуляції ФІКСОВАНИЙ (SIM_DT). Раніше
   фізику годували dt від requestAnimationFrame, і результат залежав
   від фреймрейту — тепер сервер і клієнт крокують однаково.
   ============================================================ */

import type { BlockDef, BlockId, Tier } from './types';

/* Крок фізики. Не міняти, не перерахувавши баланс:
   від нього залежить кожна траєкторія, а отже й RTP. */
export const SIM_DT = 1 / 120;

export const CONFIG = {
  cols: 10,               // ширина шахти в блоках
  /* Розмір блоку — cell = clamp(ширина_екрана / cols, minCell, maxCell).
     При 10 колонках підлога 54px на вузькому телефоні (320-430px)
     давала fieldW > ширини екрана — поле не влазило й крайні
     колонки просто зрізалися канвасом. Підлога знижена настільки,
     щоб на будь-якому реальному екрані cell рахувався від ширини,
     а не впирався в неї: 320 / 10 = 32 — усе ще вище підлоги.
     Стеля теж знижена — камера ніби відсунута далі, видно більше
     шахти одразу, а не кілька великих блоків впритул. */
  /* Межі розміру клітинки. Їх задає не смак, а зум пальцями: саме вони
     вирішують, наскільки далеко можна відвести камеру й наскільки
     близько підвести.
       minCell 12 — на телефоні (~400px) це ~33 колонки в кадрі, тобто
         усе поле з огорожею й запасом чорноти. Було 28, і віддалення
         впиралось у цю підлогу раніше за ZOOM_MIN.
       maxCell 200 — дозволяє наблизитись до кількох блоків; було 100,
         і на широкому екрані стеля спрацьовувала ще до будь-якого зуму. */
  maxCell: 200,
  minCell: 12,

  startBalance: 3000,     // dev
  bets: [10, 25, 50, 100, 250, 500, 1000, 2000],
  /* Дільник виплати — головна ручка RTP. Зв'язок оберненo лінійний
     (стеля maxWinX не спрацьовує, тож нічого не зрізає): подвоїш K —
     удвічі менша віддача.

     Ціль зараз 88%. Історія: 252 -> 428 -> 587 -> 222 -> 240 -> 216
     -> 215 -> 191 -> 188 -> 230 -> 215.

     ОСТАННІЙ СТРИБОК (188 -> 230) — не перебалансування математики, а
     плата за скаттери. Виграна ними бонуска нічого не коштує гравцю,
     але виплачує як куплена (~43 ставки в середньому), тож сама по
     собі підняла б сумарний RTP далеко за 88%. K забирає цю різницю
     назад із базової гри. Далі бонуску зрізали на ~39% (CONFIG.buy),
     тож частина грошей повернулась у звичайні прокрути: 88% тепер
     складаються приблизно як 77% базової гри + 11% бонуски.

     Ціни бонус баю перераховувати після зміни K НЕ треба: він однаково
     ділить і виплату бонуски, і базовий RTP, тож їхнє відношення
     лишається. А от зміна ваг блоків, фізики чи цінностей — це новий
     прогін sim/final.ts. */
  payoutK: 215,
  /* Стеля виграшу за раунд, у ставках. Реальний максимум на поточній
     математиці ≈ x26 на 50k ставок (p99.9 ≈ x15), тому стеля майже
     ніколи не спрацьовує — це чесний запобіжник, а не маркетингове
     число. Розширювати хвіст — це вже нова математика (крива руди,
     нові множники), а не підняття стелі. */
  maxWinX: 500,

  /* ---- рулетка ----
     ОДИН прокрут на ставку — як у справжньому 777: потягнув, побачив
     результат. Кірка випала -> гра; не випала -> ставка згоріла одразу,
     без повторних спроб.

     УВАГА: при spinsPerBet=1 шанс кірки за СТАВКУ дорівнює шансу за
     ПРОКРУТ (тепер це одне й те саме число). Раніше (spinsPerBet=7)
     кірка була за ставку в ~71% випадків при тому самому nothingWeight,
     бо це 7 незалежних шансів поспіль. Зараз із тим самим nothingWeight=84
     кірка випадає лише в ~16% ставок — суттєво рідше. Це напряму тягне
     за собою:
       - нижчий RTP (менше виграшних ставок узагалі);
       - стрік bonus.streak=12 стає практично недосяжним (0.16^12 —
         астрономічно рідко), бо він і рахувався на ~71%-ний шанс.
     Якщо потрібно повернути схожу частоту кірок — знизь nothingWeight
     (наприклад, до ~2, щоб кірка була ~89% і компенсувала втрату
     7 попередніх шансів; підбирай через sim/final.ts і sim/balance.ts). */
  spinsPerBet: 1,
  /* Ваги рулетки задають частоту запуску шахти. ≈ 15.5% за ставку:
     nothingWeight 109 проти суми кірок 20 (9+5+3+2+1), разом 129.
     Розклад за ставку: Wooden 7.0% / Stone 3.9% / Iron 2.3% /
     Golden 1.6% / Diamond 0.78%. Фінал — симуляцією з payoutK.
     Було 100 (16.7%) — кірку зроблено на 7% рідшою. */
  nothingWeight: 109,
  /* PITY: стільки пустих ставок ПІДРЯД, після яких НАСТУПНА гарантовано
     дає кірку (кожна 8-ма = 7 пустих + форс). Тір — за звичайними вагами.
     Лічильник живе на гравці ОКРЕМО по кожній ставці (PlayerRecord.dryStreaks). */
  pity: 7,
  /* Рулетка виглядає як класичний однорядний слот («777»): ОДНА
     вертикальна стрічка символів, у вікні видно три — над, на лінії
     виплати (по центру, з рамкою) і під. Це не три окремі барабани
     поруч (тоді був би горизонтальний рядок символів) — символ, що
     вирішує результат, завжди один, просто у вікні видно сусідів
     по стрічці, як у справжньому апараті. На шанси це не впливає:
     вирішує все той самий один прокрут із reelTable(). */
  reel: {
    /* Тривалість одного прокруту. Було 620 мс — ставку встигало
       розіграти раніше, ніж око встигало за стрічкою, і прокрут читався
       як мигання, а не як обертання.

       Разом із часом виріс і ШЛЯХ (targetIndex 17 -> 26): якби виріс
       лише час, та сама відстань просто поїхала б повільніше — вийшло б
       сповільнене відео, а не довший прокрут. При згасанні (1-p)^5
       стартова швидкість тепер 5*26/1.15 ≈ 113 комірок/с проти
       колишніх 137 — стрічка так само зривається з місця, але їде
       далі й сідає довше.

       Прокрут за ставку рівно один (spinsPerBet = 1), тож на довжину
       раунду це не множиться. Кнопка швидкості (x2/x3/x10) стискає це
       разом з усім іншим: dt у presenter множиться на speed. */
    spinMs: 1150,
    gapMs: 170,           // пауза між прокрутами
    /* stripLen мусить бути більшим за targetIndex рівно настільки, щоб
       під лінією виплати лишались комірки: стрічка не циклічна. */
    stripLen: 31,
    targetIndex: 26,
    /* Скільки символів видно у вікні.

       Було 3 (над лінією виплати, на ній і під) — під стару прямокутну
       рамку. Нова рамка кругла, і смуга 1:3 влазить у коло лише вузькою
       щілиною: заміряно по альфа-каналу — комірка виходила 0.195 ширини
       рамки проти 0.435 для одного символу. Тобто три видимі символи
       коштували б утричі дрібнішого символу на екрані.

       Прокрут від цього не зникає: стрічка так само летить крізь вікно,
       просто в кадрі щоразу один символ, як у медальйоні. */
    visible: 1,
    widthRatio: 1,        // квадратна комірка-символ, як у класичному слоті
    riseMs: 550,          // за скільки рулетка їде вгору, коли випала кірка
  },

  /* ---- СКАТТЕР ----
     Три однакові блоки за один забіг -> НАСТУПНА ставка стає безкоштовною
     бонускою з випадковою кіркою.

     Рахуються тільки РОЗБИТІ скаттери, а не побачені: інакше нагороду
     давав би екран, а не гра. Блок звичайної міцності (tough 1), тому
     потрапив — забрав.

     Виграш переноситься на наступний раунд, а не вмикається посеред
     поточного: забіг уже йде своєю шахтою, і підмінити її на льоту
     означало б обірвати те, що гравець зараз дивиться. */
  scatter: {
    need: 3,
    /* Вага блока в генерації — головний важіль усієї фічі.

       Безкоштовна бонуска коштує дорого: середня ціна купленої кірки
       (за вагами рулетки) ~43 ставки, тобто кожне спрацювання додає до
       RTP стільки ж, скільки сорок звичайних ставок. Тому вага тут
       мала й міняти її наосліп не можна — див. sim/scatter.ts.

       0.18 — зважений вибір із розгортки (sim/scatter:sweep): бонуска
       раз на ~219 ставок і 17.7% усього RTP. Це і є ціна, за яку
       payoutK піднято зі 188 до 230: базова гра віддає 72.5% замість
       колишніх 88%, решта пішла в бонуску. */
    weight: 0.18,
    /** з якого ряду взагалі трапляється */
    fromRow: 5,
  },

  /* ---- СТРІЛКА ВГОРУ (блок grow) ----
     Кірка ТИМЧАСОВО більшає: росте і спрайт, і РАДІУС ЗІТКНЕННЯ.

     НА RTP ЦЕ НЕ ВПЛИВАЄ, і це навмисно. Кожен дотик коштує 1 HP, тому
     скільки блоків забіг устигне розбити, вирішує запас HP, а не площа:
     ширша кірка витрачає той самий бюджет ширшим махом. Заміряно на
     4000 забігів (кірка гарантована):

       без росту               глибина 77.8 | блоків 269 | 45.2 с |  742 очки
       більша, HP не чіпали    глибина 73.2 | блоків 268 | 43.8 с |  743 очки
       більша + запас HP ×3    глибина 82.3 | блоків 321 | 51.0 с | 1010 очок

     Тобто віддачу тягнув множник HP, а не розмір. Множник прибрано:
     блок лишається видовищним, а математика на місці.

     СТАКАЄТЬСЯ. Кожне спіймане збільшення живе sec секунд НЕЗАЛЕЖНО
     від решти, а розміри перемножуються: два поспіль -> ×4, три -> ×8.
     Таймери окремі, тож ефект і спадає сходинками, а не разом. */
  grow: {
    scale: 2,
    sec: 5,
    /* Стеля одночасних збільшень. Причина фізична, не балансна: поле
       завширшки cols, а радіус кірки — bodyR * scale. На трьох стаках
       (×8) діаметр ≈ 8.8 клітинки, тобто кірка вже майже на всю ширину
       шахти й притиснута стінками до центру — рости просто нікуди.
       Плюс перебір клітинок у collide() росте квадратично. */
    maxStacks: 3,
  },

  /* ---- ГУМОВИЙ БЛОК ----
     ТРАМПЛІН. kick — АБСОЛЮТНИЙ імпульс відскоку в клітинках/с, а не
     надбавка до звичайного bounceKick.

     Чому саме так: bounceKick навмисно слабкий (1.4) — кірка важка, і
     звичайні удари не мають її підкидати. Спроба зробити гуму «на 25%
     сильнішою за звичайний відскок» давала +25% до слабкого, тобто
     знову слабке: на екрані з'являвся напис, а кірка майже не рухалась.
     Абсолютний імпульс від цього не залежить.

     13.8 клітинок/с при гравітації 29 — підйом ≈ 3.3 клітинки.

     speedUp — уся дуга (зліт І падіння назад) грається у стільки разів
     швидше. Це саме ПРИШВИДШЕННЯ ЧАСУ, а не сильніший стрибок, тому
     множники різні: швидкості на speedUp, а прискорення на speedUp²
     (h = v² / 2g — обидва квадрати скорочуються, і висота лишається
     та сама). Якби гравітацію просто подвоїли, стрибок став би вдвічі
     нижчим замість того щоб стати вдвічі коротшим у часі.
     Дуга: 0.9 с -> 0.48 с при тій самій висоті. */
  rubber: {
    /* Відскок як у слизі в майнкрафті, але слайм тут АКТИВНИЙ трамплін:
       restitution > 1, тобто він віддає більше, ніж у нього прилетіло.
       Для гри це нормально — енергію додає сам блок, — а от рахувати
       висоту стрибка «на око» через це не можна, бо під час дуги ще й
       час стиснуто: gravity множиться на speedUp² (див. stepPick), тому
       підйом = v² / (2 * gravity * speedUp²) = v² / 125.

       Звідси й числа: удар на повній швидкості падіння (maxFall 20)
       дає 20 * 1.3 = 26 -> * speedUp = 36.4 -> підйом ≈ 10.6 клітинки;
       найлегший дотик через minKick — близько 3.5. До підсилення було
       5.6 і 1.3 відповідно. Раніше kick був константою, тому легкий
       дотик підкидав рівно так само, як падіння з висоти, — і кірку
       щоразу запускало вгору незалежно від того, що відбувалось.
       Тепер повільно торкнувся — легко відскочив, впав здалеку —
       підстрибнув високо. */
    restitution: 1.3,
    /* Щоб і зовсім повільний дотик усе-таки відштовхував, а не гасив
       кірку об блок. */
    minKick: 15,
    /* Стеля: падіння на maxFall інакше давало б відскок на пів екрана. */
    maxKick: 30,
    /* Пришвидшення дуги (швидкості на speedUp, прискорення на його
       квадрат — див. коментар нижче в stepPick). Було 2 — разом із
       фіксованим імпульсом це й давало відчуття «дуже сильно». */
    speedUp: 1.4,
    /* Стеля бічної швидкості для трампліна — у частках від
       P.maxSideSpeed. Окрема ручка, бо speedUp — це ПРИШВИДШЕННЯ ЧАСУ
       дуги, а не сила; вішати на нього ще й стелю означало б, що
       спроба вкоротити стрибок у часі мовчки послаблює його вбік.
       Без піднятої стелі удар у слайм збоку впирався у звичайні 10, і
       «сильніше» було видно тільки вгору. */
    sideBoost: 2.4,
    fallSec: 1.0,
    /* СМУГА СПАВНУ — частка ширини шахти по центру, поза якою слайм не
       кладеться взагалі (world.ts, genRow).

       Причина не декоративна. Трамплін біля стінки штовхає кірку по
       нормалі дотику, а найближча поверхня там — сама стінка: кірка
       відскакувала в неї, гасла об wallBounce і сповзала вздовж краю,
       замість того щоб піти назад у поле. Чим сильніший відскок, тим
       помітніше — а він тепер сильніший.

       0.5 при cols = 10 лишає слайму колонки 2..7: по дві крайні з
       кожного боку вільні. Побічний ефект — слаймів стає менше
       приблизно на 40%, бо вага (rubber у depthWeights) лишилась та
       сама; якщо їх поменшає надто помітно, піднімати треба саме її. */
    centerFrac: 0.5,
  },

  /* ---- БОНУС БАЙ ----
     Гравець платить наперед і ГАРАНТОВАНО отримує обрану кірку —
     рулетка не крутиться, промаху не буває.

     price — множник до ставки, СВІЙ на кожну кірку: алмазна за той
     самий цінник, що й дерев'яна, була б безкоштовними грошима.
     Числа підібрані симуляцією так, щоб віддача бонуски збігалася зі
     звичайною грою — купівля має бути іншим ТЕМПОМ, а не вигіднішою
     ставкою. Міняєш HP/урон кірок, ваги блоків чи payoutK — переганяй
     sim/buy.ts і онови ці числа.

     multBoost — наскільки щедріша бонусна шахта. Раніше важелів було
     два (другим був enchantBoost, частота столів зачарування), але
     зачарування прибрано як механіку, тож лишився один: ЄДИНЕ, чим
     бонусна шахта тепер відрізняється від звичайної, — частота
     блоків-множників. Це окремий ризик: перевірити симуляцією, що
     бонуска й далі відчувається щедрішою. */
  buy: {
    /* Наскільки щедріша бонусна шахта проти звичайної. Тут і тільки
       тут живуть усі підняття: у звичайній грі ваги лишаються базовими. */
    /* Зрізано на ~25% (4 -> 3, хвіст нижче теж): бонуска мала
       віддавати менше — і куплена, і виграна скаттерами. Оскільки
       обидві беруть ту саму шахту, важіль тут один на дві фічі. */
    multBoost: 3,        // частота блоків-множників: 1.6 -> 3
    /* Стеля стакнутого множника бонуски. Номінали складаються, тож
       росте він лінійно і в цю стелю практично не впирається — вона
       тут як запобіжник на випадок аномально довгого забігу, а не як
       робоче обмеження. */
    multCap: 250,
    /* У скільки разів важчий кожен ВЕЛИКИЙ ікс у бонусній шахті.
       Дрібні (x2, x3, x5) не чіпаємо — вони просто втрачають частку.
       У звичайній грі всі ваги лишаються базовими. */
    tailBoost: { 10: 1.9, 15: 1.6, 25: 1.4, 50: 1.4 } as Record<number, number>,
    /* ЦІНИ ЗВЕДЕНІ НЕ НА РІВЕНЬ БАЗОВОЇ ГРИ, А НИЖЧЕ — навмисно.

       Раніше вони давали ту саму віддачу, що й звичайний прокрут:
       купівля була іншим темпом, не гіршою ставкою. Тепер бонуска
       свідомо віддає менше — і куплена, і виграна скаттерами.

       Ціль ~70% проти 78% базової гри. Числа = середня виплата / 0.70,
       заміряно sim/buy.ts на 20k забігів при payoutK 215 і зрізаній
       щедрості шахти (multBoost 3).

       ВАЖЛИВО: лишити старі ціни було НЕ МОЖНА. Виплати впали на ~39%,
       а ціни ні — куплена бонуска віддавала б 52-59%. Це вже не
       «менший RTP», а цінник, який помітять з першої купівлі.

       Алмазна коштує трохи більше за чесну навмисно: вона заробляє
       практично стільки ж, скільки золота (x64.0 проти x65.3 —
       різниця в межах шуму), і якби ціна йшла строго за заміром,
       верхній тір виявився б дешевшим за попередній. Сходинка вниз у
       прайсі читається як помилка.

       ПЕРЕРАХОВУВАТИ після будь-якої зміни фізики, ваг блоків або
       щедрості бонусної шахти — саме вони й рухають ці числа. */
    price: {
      lvl2: 9.3,     // середня виплата x6.48
      lvl3: 27.5,    // x19.31
      lvl4: 58,      // x40.91
      diamond: 94,   // x64.03
    } as Record<string, number>,
  },

  /* ---- ВЕРСТАК ----
     Топ-тір (Diamond): 1-й верстак лікує повністю, кожен наступний —
     лише +topHeal HP. Нижчі тіри верстак підвищує на рівень + повний хіл. */
  workbench: {
    topHeal: 50,
  },

  /* ---- БЛОКИ-МНОЖНИКИ ----
     Бонуску прибрано. Множник тепер відкриває ВІКНО (MULT_WINDOW_SEC
     секунд, див. run.ts): поки воно активне, усе зібране множиться на
     активний множник. Новий множник під час вікна — беремо більший. */
  mult: {
    weight: 0.25,        // вага блоку-множника серед фонових блоків (звичайна гра)
    /* ЗВИЧАЙНА ГРА. Що більший ікс — то менший шанс. Максимум x50.

       Ці ваги навмисно лишились такими, як були: підняття хвоста живе
       ТІЛЬКИ в бонусці (CONFIG.buy.tailBoost), інакше воно тягло б RTP
       звичайної гри — перевірено, з піднятим хвостом він стрибав до
       117%. Нові x25 і x50 тут теж рідкісні: верхівка хвоста має
       лишатись рідкістю, інакше вона перестає нею відчуватись.

       Найсильніша ручка верхніх виплат — після будь-якої зміни
       переганяти sim/final.ts. */
    table: [
      { m: 2, weight: 58 },
      { m: 3, weight: 26 },
      { m: 5, weight: 11 },
      { m: 10, weight: 4 },
      { m: 15, weight: 1 },
      { m: 25, weight: 0.5 },    // рідкісний
      { m: 50, weight: 0.12 },   // дуже рідкісний, верхівка хвоста
    ],
  },

  /* ---- фізика (в клітинках за секунду) ---- */
  phys: {
    gravity: 32,          // 33 -> 26 -> 29 -> 32: кірку щоразу робили важчою
    maxFall: 20,          // було 27
    /* ---- ДОТИК ----
       Відскок рахується розкладом швидкості на НОРМАЛЬ дотику й
       ДОТИЧНУ до неї (Run.resolveContact). До цього була бінарна гілка
       «збоку / зверху» і фіксований бічний поштовх — саме звідти й
       бралося те, що кірку безпричинно носило по сторонах.

       restitution б'є ТІЛЬКИ по нормальній складовій: скільки швидкості
       «в поверхню» повертається назовні. За бічний рух він більше не
       відповідає — його дає тертя (нижче), тому підбирати одне, не
       ламаючи інше, тепер можна. */
    restitution: 0.42,
    /* Поштовх «врубання» — ПО НОРМАЛІ, а не вбік. Кірка не просто
       пружинить: вона впирається в блок і відштовхується від нього.
       Напрямок нормальний, тому сам по собі цей поштовх бічного дрейфу
       не створює взагалі — тільки відводить кірку від поверхні.
       2.6 -> 2.0 -> 1.4 -> 2.8 -> 5.0. ЦЕ Й Є ГОЛОВНИЙ ВАЖІЛЬ БІЧНОГО РУХУ, і
       це не суперечить сказаному вище: поштовх іде по нормалі, а нормаль
       вертикальна лише коли кірка лягла на пласке дно. Щойно вона
       зачепила грань або кут збоку — той самий поштовх штовхає вбік.

       Заміряно (шахта завширшки 10, пікове відхилення від точки старту):
       1.4 -> 1.65 клітинки (кірка фактично свердлила вниз), 2.8 -> 2.88,
       5.0 -> 4.01. Вище 5.0 віддача згасає: 5.4 дає ті самі 4.06, лише
       різкіше.

       Чому це НЕ «відкидає». Підйом від поштовху — v²/2g, тобто при 5.0
       і гравітації 29 це 0.43 клітинки, менше половини блока; та ще й
       зверху стоїть maxRise (6.5), якого поштовх сам по собі не дістає.
       Бічну складову ніщо не зрізає, тому число йде майже повністю в
       ширину, а не в підкидання. Стеля maxSideSpeed = 10 теж не
       вибирається: середня |vx| близько 4. */
    bounceKick: 5.0,
    /* Стеля швидкості ВГОРУ після звичайного відскоку. Кірка важча, і
       «свічки» на пів екрана від одного удару в підлогу виглядають
       неправильно — вертикальний відскок обрізається тут, а не
       підбором restitution.
       Вибух TNT цією стелею не обмежений — там підкид навмисний. */
    maxRise: 6.5,
    /* ---- ТЕРТЯ НА ДОТИКУ (звідки тепер береться діагональ) ----
       Кулонова модель: дотична сила не може перевищити friction від
       нормального імпульсу. Що м'якше кірка торкнулась — то менше
       вона може зачепитись за грань, і навпаки.

       Це ЗАМІНА старим sideKick / sideKickRand. Принципова різниця не
       в числах, а в знаку зворотного зв'язку:

         - sideKick ДОДАВАВ бічну швидкість на кожному дотику, завжди в
           той самий бік від центра блоку. Енергія бралась з нічого, і
           чим більше кірка стукалась, тим сильніше її розганяло вбік —
           додатний зворотний зв'язок, звідси й «носить».
         - тертя ЗАБИРАЄ ковзання: якщо кірка вже летить убік, дотик її
           гальмує, а гальмування переходить в обертання. Бічний рух
           став самообмежним.

       grip — яку частку ковзання гасимо, коли запасу тертя вистачає
       (1 = дотик намертво чіпляється, 0 = ідеально слизька грань).

       ЧОМУ 0.10, А НЕ БІЛЬШЕ. Спокуса підняти тертя заради «більшого
       розмаху» оманлива, і це перевірено заміром: 0.16 -> 0.24 дає
       БІЛЬШЕ обертання (1.26 проти 0.97 рад/с), але МЕНШЕ бічного
       ходу — пікове відхилення падає з 1.65 до 1.75..1.25, бо та сама
       дотична сила, яка перетворює спін на рух, спершу гасить уже
       набране ковзання. Тертя тут — гальмо дрейфу, а не його джерело.
       Джерело — bounceKick (див. вище); тертя лишаємо низьким, щоб
       набране вбік не з'їдалось об наступну ж грань. */
    friction: 0.10,
    grip: 0.85,
    /* ---- ОБЕРТАННЯ ЯК ЧАСТИНА ФІЗИКИ, А НЕ АНІМАЦІЯ ----
       Головка важча за ручку, тому центр мас зміщений від геометричного
       центра В БІК ГОЛОВКИ рівно на comOffset (у частках клітинки,
       росте разом зі scale).

       Через це нормальний імпульс проходить НЕ через центр мас, щойно
       кірка торкається грані не строго головкою — і створює момент.
       Кірку перевертає, перевертання через тертя перетворюється на
       ковзання вздовж грані, а ковзання — знову на обертання.

       Саме цей обмін і дає рух по діагоналі. Він читається на екрані:
       кірку зносить туди, куди її щойно крутнуло, а не в випадковий бік.
       Побічний ефект — падіння строго головкою вниз стає нестійким
       (як у справжнього молотка), тож кірка не залипає у вертикаль.

       spinInertia — обернений момент інерції: на скільки rad/s
       розкручує момент в одиницю. Більше = кірка охочіше перекидається
       і сильніше гуляє вбік. */
    comOffset: 0.18,
    spinInertia: 2.4,
    wallBounce: 0.88,     // відскок від бічної стінки шахти. Було 0.7 —
                          // занадто слабкий, кірка гасла біля стінки й не
                          // мала енергії повернутись через усе поле, тому
                          // осідала близько до країв. Тепер відбивається
                          // енергійніше — рух живіший, динамічніший.
    airDrag: 0.14,        // гальмування по горизонталі. Було 0.25 -> 0.15 -> 0.18.
                          // 0.14: після переходу на тертя бічний рух став
                          // самообмежним, тож гасити його ще й повітрям
                          // не треба — розгону «з нічого» більше нема.
    maxSideSpeed: 10,     // було 9 -> 11, тепер трохи менше за пікове значення
    /* Розкрут від подій, які НЕ є дотиком грані: вибух TNT і зрив із
       вертикалі. Звичайний удар сюди більше не заходить — його момент
       рахується з геометрії дотику (comOffset / spinInertia вище), а не
       береться навмання. */
    /* ---- ЗРИВ ІЗ ВЕРТИКАЛІ ---- (застосування — stepPick у run.ts)
       Кірка охоче звалюється «під себе»: пробила колонку блоків і далі
       падає тим самим тунелем. Тому лічимо час майже вертикального руху
       і після straightSec штовхаємо її вбік.

       straightVx — нижче цього |vx| рух вважається вертикальним;
       straightPush — база бічного імпульсу (затравка, далі підхоплює
       звичайна фізика дотику). */
    straightSec: 0.6,
    straightVx: 1.3,
    /* Зрив переважно КРУТИТЬ кірку, а не штовхає: straightSpin великий,
       straightPush маленький. Так і задумано — бічний рух має бути
       наслідком видимого кувирка через тертя на першому ж дотику, а не
       ривком нізвідки. Замір це підтверджує: пара (push 3.0, spin 1.7)
       дає пік відхилення 3.00 клітинки, а (push 1.2, spin 4.2) — 3.40,
       і при цьому середня |vx| МЕНША (2.62 проти 2.79). */
    straightPush: 1.2,
    straightSpin: 4.2,
    spinKick: 1.2,
    spinDamp: 1.4,        // гасіння обертання.
    maxSpin: 3.5,         // стеля |кутової швидкості| (rad/s).
    /* Кірка — маятник: головка важча за ручку, тому її «тягне вниз».
       Слабка пружина до restRot + spinDamp -> кірка гойдається і
       влягається головкою донизу, а не завмирає під випадковим кутом.
       БІЛЬШЕ НЕ КОСМЕТИКА: кут визначає, де центр мас і яким кінцем
       кірка зустріла грань, тобто і силу відскоку (headBounce /
       handleBounce), і момент від удару. Те, що гравець бачить як
       кувирок, і є те, що жене кірку вбік. */
    restRot: 2.3,         // кут «головкою вниз» для спрайту (rad)
    /* ---- ЧИМ САМЕ ВДАРИЛА ----
       Кірка — не куля: у неї є важка ГОЛОВКА і легка РУЧКА. Удар
       головкою має бути глухим (вона важча, інерція гасить відскок),
       удар ручкою — пружним, кірку відкидає помітніше.

       headBounce / handleBounce — множники сили відскоку для цих двох
       випадків; між ними значення інтерполюється за тим, наскільки
       напрямок головки збігається з напрямком НА ГРАНЬ. Кут головки
       береться з rot: при rot = restRot вона дивиться вниз.

       ЗНАК ТУТ БУВ ПЕРЕВЕРНУТИЙ. Порівнювали напрямок головки з
       вектором ВІД блоку ДО кірки — тобто зі спрямованим геть від
       грані. Через це найчастіший удар у грі (кірка висить головкою
       вниз, як її і тримає маятник, і падає на блок під собою)
       розпізнавався як удар РУЧКОЮ й отримував handleBounce = 1.3
       замість headBounce = 0.62. Кожен звичайний дотик відкидав кірку
       вдвічі сильніше, ніж мав, — це й була головна причина, чому її
       носило по полю. Тепер порівнюємо з нормаллю дотику зі знаком
       мінус (нормаль теж дивиться від грані).

       На виграш це не впливає прямо — міняється лише траєкторія, а
       отже й те, скільки блоків кірка встигне зачепити. */
    headBounce: 0.62,
    handleBounce: 1.3,
    /* Велика кірка (блок-стрілка) стрибає гірше: маса росте швидше за
       силу удару. На кожен крок збільшення відскок множиться на цю
       частку — при scale 8 виходить приблизно половина від звичайного. */
    sizeDamp: 0.82,
    rotPull: 3.5,         // жорсткість пружини до restRot
    bodyR: 0.55,          // радіус тіла кірки в частках клітинки. Було 0.36 —
                          // колізія ловила тільки крихітну точку в напрямку
                          // польоту, і візуально кірка (разом з ручкою) вже
                          // торкалась блоку, а гра ще ні. Тепер зачіпає
                          // майже всю площу спрайту — колізія й на ручці.
    /* ВІДСКОК ВІД БЛОКУ, ЩО ВИЖИВ І ЛЕЖИТЬ ПІД КІРКОЮ.
       Удар, який блок ПЕРЕЖИВ (накопиченого урону ще менше за tough —
       гілка 'crack' у Run.impact) І прийшовся переважно ВНИЗ, відкидає
       кірку рівно в стільки разів слабше. Блок, що розлетівся, і будь-який
       дотик ЗБОКУ відкидають на повну, без множника.

       Напрямок тут вирішує. Униз кірку тримає власна вага, тож слабкий
       відскок читається як врубання. Збоку її не тримає ніщо: заглушений
       удар у вертикальну грань виглядав би як зависання в повітрі
       впритул до жили.

       Сенс простий: кірка не має відскакувати від руди, яку вона ще
       довбає. Раніше будь-який дотик по міцному блоку жбурляв її геть,
       і замість «прогризла жилу» виходило «постукала й відлетіла».

       Множник б'є по ОБИДВОХ складових нормального імпульсу — і по
       пружині (|vn| * restitution), і по фіксованому поштовху
       (bounceKick). Це навмисно: гасити треба саме поштовх, бо він не
       залежить від швидкості удару й на слабких дотиках якраз і давав
       те безпричинне підкидання.

       Дотичну складову множник НЕ чіпає: кірка, яка довбає жилу, має
       по ній зісковзувати, а не приклеюватись.

       Кірка від цього не застрягає: тяжіння далі тягне її вниз,
       Run.separate() тримає її на поверхні блоку, а кожен наступний
       дотик через hitCooldown додає урон — вона просто ПРОГРИЗАЄ блок
       замість того щоб від нього стрибати. */
    crackBounce: 0.2,
    substep: 0.22,        // максимальний крок інтегрування в клітинках
    hitCooldown: 0.05,    // мінімум часу між ударами по одному блоку
    /* Сила відкиду від вибуху TNT. Було 6.0 — при гравітації 29 це
       підйом на 0.6 клітинки, тобто на екрані вибух кірку практично не
       рухав. 10.5 дає до ~1.9 клітинки: помітно, але не «свічка».
       Напрямок рахується від епіцентру — див. impact() у run.ts. */
    tntBlast: 10.5,
    startHeight: 1.5,     // з якої висоти падає кірка (нижче — «випадає» з-під рулетки)
    /* Довжину забігу тримає HP кірки (+ хіли верстака). maxHits/maxTime —
       лише страховка від зациклення, НЕ ігровий обмежувач: раніше 900/240
       рубали живу кірку на x5..x11 «просто так». */
    maxHits: 4000,
    maxTime: 420,         // секунд симуляції
    maxSteps: 200000,     // жорстка стеля кроків симуляції на сервері
  },

  /* ---- КАМЕРА ----
     Камера більше не «стеля, що повзе вниз», а стежить за кіркою по
     ОБОХ осях і тримає її по центру екрана. Тому вона їде і вгору, і
     вбік — гравець бачить не смугу шахти, а ділянку поля навколо кірки.

     viewCols — скільки колонок влазить по ширині. МЕНШЕ за cols (10)
     навмисно: якби на екран влазило все поле, стежити камері не було б
     за чим — вона стояла б на місці. Тепер поле ширше за екран, камера
     реально панорамує, а біля країв видно огорожу.
     Історія: 7 -> 8 -> 9.6 (камеру щоразу відсували на прохання;
     останній крок — рівно +20%). */
  viewCols: 9.6,
  camLead: 0.5,           // де тримати кірку по вертикалі екрана (0.5 = центр)
  camIdle: 0.80,          // поки крутиться рулетка — поверхня отут (частка висоти екрана)
  camLerp: 6,
} as const;

/* ---------- КІРКИ ----------
   hp     — запас міцності кірки: скільки ударів вона взагалі витримає.
            cost блоку тепер уніфіковано (1 для будь-якого блоку, див. BLOCKS
            нижче), тож hp напряму й є цільова кількість ударів на кірку.
            Підняте відносно попереднього — довше «життя» кірки за проханням.
   dmg    — УРОН за удар: чим вища кірка, тим швидше довбає руду
            (ударів по блоку = ceil(BLOCKS[*].tough / dmg))
   weight — шанс випасти на прокруті, ВІДНОСНО nothingWeight (див. нижче)
*/
/* ТІРІВ ЧОТИРИ. Золота кірка (weight 2, hp 300, dmg 5) була п'ятою,
   між Iron і Diamond, — її прибрано з проєкту разом зі скіном і ціною
   в бонус баї. Ваги решти НЕ перерозподіляли: сума ваг кірок впала з
   20 до 18 проти nothingWeight = 109, тобто кірка тепер випадає в
   18/127 = 14.2% ставок замість 15.5%. Це рухає RTP — переганяй
   sim/final.ts і підбирай payoutK. */
/* СКІНИ. Нові кірки намальовані ВПРИТУЛ до країв полотна 256x256
   (по діагоналі з кута в кут), старі були 160x160 з полями (малюнок
   займав 0.8125 полотна). Клієнт це знає й рахує екранний розмір від
   ЧАСТКИ полотна, а не від полотна — див. PICK_FILL в
   apps/web/src/game/assets.ts. Тому міняти bodyR не довелось.

   У золотої кірки нової картинки поки немає — вона лишається зі
   старого комплекту (160x160). */
export const TIERS: readonly Tier[] = [
  { id: 'lvl2', name: 'Wooden', weight: 9, hp: 100, dmg: 2, color: '#a9803f', color2: '#6b4a20',
    skin: '/pickaxes/pickaxe_0.png' },
  { id: 'lvl3', name: 'Stone', weight: 5, hp: 150, dmg: 3, color: '#a8a8a8', color2: '#6e6e6e',
    skin: '/pickaxes/pickaxe_1.png' },
  { id: 'lvl4', name: 'Iron', weight: 3, hp: 200, dmg: 4, color: '#e2e2e2', color2: '#9d9d9d',
    skin: '/pickaxes/pickaxe_2.png' },
  { id: 'diamond', name: 'Diamond', weight: 1, hp: 400, dmg: 7, color: '#57eede', color2: '#22b7a8',
    skin: '/pickaxes/diamond_pickaxe.png' },
];

export const TIER_BY_ID: Record<string, Tier> =
  Object.fromEntries(TIERS.map((t) => [t.id, t]));

export const tierIndex = (id: string) => TIERS.findIndex((t) => t.id === id);

/* Порожній результат прокруту. Іконка поки просто «Х». */
export const NOTHING = { id: 'none', name: 'Пусто', color: '#39424f', color2: '#242b35' } as const;

/* ---------- БЛОКИ ----------
   tough — МІЦНІСТЬ блоку. Кірка знімає свій dmg за удар, блок падає
           коли накопичений урон >= tough.
           Земля, камінь, динаміт мають tough 1 — б'ються ЗАВЖДИ з одного
           удару будь-якою кіркою. Міцність є тільки в руди.
   cost  — скільки HP кірки з'їдає ОДИН дотик, уніфіковано: 1 для будь-якого
           блоку (раніше 3-8 залежно від блоку). tough на це не впливає —
           руда й далі вимагає кількох ударів, просто кожен коштує так само.
           TNT кірці шкоди не завдає (cost 0) — вибух безкоштовний.
   value — скільки очок дає розколотий блок (ділиться на payoutK у виплаті,
           а сама виплата множиться на bet — тому чим більша ставка, тим
           дорожчий той самий блок).
           Земля й камінь — суцільний наповнювач шахти, у виграш не йдуть
           (value 0). Прогресія за цінністю:
             Уголь 1 < Железо 5 < Лазурит 10 < Редстоун 15 < Золото 20
             < Алмаз 45 < Изумруд 130.
           Спред ×130 — щоб глибокий забіг з ізумрудом відчувався як
           jackpot. payoutK підганяється симуляцією під RTP 95%.

   ВЕРСТАК (id magic, kind 'upgrade') — прямий дотик підвищує тір
   (поки є куди рости) і лікує до максимуму; на топ-тірі (Diamond)
   щоразу дає повний хіл (довжину забігу тримають timeout + maxHits).
   Вибух TNT просто ламає його, без ефекту.

   Ім'я id 'magic' історичне й невдале: колись поруч жив ДРУГИЙ блок —
   стіл зачарування (id 'enchant', kind 'magic'), який множив виграш
   кірки. Його прибрано як механіку цілком, лишився тільки верстак.
   Перейменовувати id не стали: він їздить у збережених раундах.
*/
/* РУДА ЗАЛІЗА, ЗОЛОТА Й РЕДСТОУНУ малюється двома шарами: булижник
   (skin нижче) + накладка з рудою поверх нього. Художник дав саме
   накладки, а не готові блоки, тому skin у них — cobble, а накладку
   підмішує Render.block (мапа ORE_OVERLAY). */
export const BLOCKS: Record<BlockId, BlockDef> = {
  grass:    { id: 'grass',    name: 'Дёрн',     kind: 'solid', tough: 1, cost: 1, value: 0,  color: '#5f8a3f', skin: '/blocks/grass.png' },
  dirt:     { id: 'dirt',     name: 'Земля',    kind: 'solid', tough: 1, cost: 1, value: 0,  color: '#8a5f38', skin: '/blocks/dirt.png' },
  stone:    { id: 'stone',    name: 'Камень',   kind: 'solid', tough: 1, cost: 1, value: 0,  color: '#8f8f8f', skin: '/blocks/stone.png' },
  /* Вартості зрізано рівно на 15% (× 0.85) від попередніх цілих:
     1 / 5 / 10 / 15 / 20 / 45 / 130. Дроби лишені навмисно — округлення
     до цілих зіпсувало б саме те, заради чого правка робиться: вугілля
     з 0.85 стало б 1 (нуль зрізу), а редстоун із 12.75 -> 13 (-13%
     замість -15%). value ніде не показується як є: у виплату йде
     value * ставка / payoutK, тож дробове значення нічого не ламає. */
  coal:     { id: 'coal',     name: 'Уголь',    kind: 'solid', tough: 2,  cost: 1, value: 0.85,  color: '#5f5f5f', skin: '/blocks/coal.webp' },
  iron:     { id: 'iron',     name: 'Железо',   kind: 'solid', tough: 4,  cost: 1, value: 4.25,  color: '#b98b6c', skin: '/blocks/cobble.png' },
  lapis:    { id: 'lapis',    name: 'Лазурит',  kind: 'solid', tough: 5,  cost: 1, value: 8.5,   color: '#1f4fa8', skin: '/blocks/lapis.jpg' },
  redstone: { id: 'redstone', name: 'Редстоун', kind: 'solid', tough: 5,  cost: 1, value: 12.75, color: '#b3241f', skin: '/blocks/cobble.png' },
  gold:     { id: 'gold',     name: 'Золото',   kind: 'solid', tough: 6,  cost: 1, value: 17,    color: '#e8c33a', skin: '/blocks/cobble.png' },
  diamond:  { id: 'diamond',  name: 'Алмаз',    kind: 'solid', tough: 9,  cost: 1, value: 38.25, color: '#4fe6e0', skin: '/blocks/diamond.png' },
  emerald:  { id: 'emerald',  name: 'Изумруд',  kind: 'solid', tough: 10, cost: 1, value: 110.5, color: '#16c96a', skin: '/blocks/emerald.png' },
  tnt:      { id: 'tnt',      name: 'TNT',      kind: 'tnt',   tough: 1, cost: 0, value: 0,  color: '#d63b1f', skin: '/blocks/tnt.png' },
  magic:    { id: 'magic',    name: 'Верстак',           kind: 'upgrade', tough: 1, cost: 1, value: 0, color: '#c8a165', skin: '/blocks/magic.png' },
  // блок-множник. Множник (x2, x3...) лежить у самій клітинці
  mult:     { id: 'mult',     name: 'Множитель', kind: 'mult',  tough: 1, cost: 1, value: 0,  color: '#c9a227' },
  /* Стрілка вгору — кірка більшає втричі (CONFIG.grow.scale) разом із
     запасом HP. Рідкісний і глибокий: ефект сильний. */
  grow:     { id: 'grow',     name: 'Рост',      kind: 'grow',   tough: 1, cost: 1, value: 0,  color: '#3ad1a0', skin: '/blocks/grow.png' },
  /* Слайм — трамплін. НЕ ЛАМАЄТЬСЯ: кірка від нього відскакує й летить
     далі, а блок лишається на місці, тож на нього можна натрапити знову.
     Через це ж дотик не коштує HP (cost 0, як у TNT): інакше застрягти
     біля слайму означало б безкоштовно втрачати запас ходу, а сам блок
     нічого не дає натомість. tough тут ні на що не впливає — блок ніколи
     не чистять.

     color — колір ІСКОР і рядка в лозі, не блоку: сам блок малюється
     картинкою. Був рожевий під намальований кодом слайм; новий
     скін зелений, тож і бризки тепер зелені. */
  rubber:   { id: 'rubber',   name: 'Слайм',     kind: 'rubber', tough: 1, cost: 0, value: 0,  color: '#5fbe3a', skin: '/blocks/rubber.png' },
  /* Скаттер. Грошей не дає взагалі (value 0) — уся його цінність у
     тому, що три штуки за забіг відкривають безкоштовну бонуску.
     Ламається з першого удару: полювання за ним має впиратись у те,
     чи долетить кірка, а не в те, чи проб'є. */
  scatter:  { id: 'scatter',  name: 'Скаттер',   kind: 'scatter', tough: 1, cost: 1, value: 0,  color: '#ff7a1a', skin: '/blocks/scatter.png' },
};

/* ---------- ГЕНЕРАЦІЯ ГЛИБИНИ ----------
   Шахта нескінченна. Вага кожного блоку — функція від номера ряду.
   ramp(r, a, b): 0 до ряду a, 1 після ряду b, лінійно між ними.
*/
export function ramp(r: number, a: number, b: number): number {
  return Math.max(0, Math.min(1, (r - a) / (b - a)));
}

/* Фонові блоки — на клітинку незалежно, як і раніше. Руда (coal/iron/
   gold/diamond) сюди більше НЕ входить: вона лягає покладами, а не
   випадковими цятками — див. ORE_VEINS і world.ts. */
export function depthWeights(r: number, bonus = false): Record<string, number> {
  /* Бонусна шахта (куплена кірка) — щедріша: частіші блоки-множники.
     Це ЄДИНА різниця в генерації; руда, TNT і решта лишаються такими
     самими, щоб бонуска не перетворювалась на іншу гру. Другим
     підняттям тут була частота столів зачарування — механіку
     прибрано, і разом із нею звільнилась її вага (0.15, а в бонусній
     шахті 0.30): тепер вона перерозподіляється на решту блоків. */
  const multW = CONFIG.mult.weight * (bonus ? CONFIG.buy.multBoost : 1);
  return {
    // шахта суцільна — порожніх клітинок при генерації немає (дірки
    // лишаються тільки там, де кірка пробила або рвонув TNT).
    air: 0,
    // перший шар — суцільний дерн (трава), потім звичайна земля, і вже
    // тоді пробивається камінь — як у майнкрафті
    grass: r === 0 ? 100 : 0,
    dirt:  r === 0 ? 0 : 70 * (1 - ramp(r, 0, 6)),
    stone: r === 0 ? 0 : 30 + 45 * ramp(r, 0, 6),
    tnt:     r < 3 ? 0 : 1.25,   // −50%: динаміту було 2.5
    magic:   r < 3 ? 0 : 0.38,   // верстак. Підвищує тір. 0.40 -> 0.36 -> 0.38 (+6%)
    mult:    r < 2 ? 0 : multW,
    // гума — трамплін, зустрічається помірно часто
    rubber:  r < 4 ? 0 : 0.39,   // слайм-трамплін. 0.35 -> 0.39 (+11%)
    // стрілка вгору — рідкісна й глибока: втричі більша кірка це багато
    grow:    r < 8 ? 0 : 0.08,
    /* Скаттер. Не з першого ряду: інакше три штуки збиралися б у
       верхньому шарі, куди кірка потрапляє завжди, і бонуска перестала
       б бути подією. Вага однакова в звичайній шахті й у бонусній —
       ретригер має бути таким самим рідкісним, як і перший захід. */
    scatter: r < CONFIG.scatter.fromRow ? 0 : CONFIG.scatter.weight,
  };
}

/* ---------- РУДА — ПОКЛАДАМИ, А НЕ ХАОТИЧНО ----------
   Кожна клітинка руди незалежно — то одинокі цятки без форми. Тепер
   шахта поділена на регіони по ORE_REGION_ROWS рядів (на всю ширину),
   і для кожного регіону окремо вирішується: чи є в ньому поклад даного
   типу руди, де його центр і скільки в ньому клітинок — суцільна пляма.

   Розмір і частота покладу залежать від вартості руди (BLOCKS[x].value):
   дешеве вугілля лягає великими й частими жилами, дорогий діамант —
   рідкісними й дрібними вкрапленнями. chance(midRow) — імовірність, що
   В ЦЬОМУ РЕГІОНІ ВЗАГАЛІ Є поклад (не на клітинку, а на регіон), і вона
   густішає з глибиною й виходить на плато — тим самим ramp(), що й вище.

   ВАЖЛИВО ДЛЯ ДЕТЕРМІНІЗМУ: regionId рахується від номера ряду
   (floor(r / ORE_REGION_ROWS)), а сам поклад — детермінований лише від
   (root, regionId, тип руди). Тому не має значення, який ряд регіону
   запросили першим (world.ts.regionOreMap()). */
export const ORE_REGION_ROWS = 6;

export interface VeinSpec {
  min: number;
  max: number;
  chance: (midRow: number) => number;
}

/* Усі поклади — min щонайменше 2: жоден тип руди не з'являється
   одинокою клітинкою. */
/* Значно збільшено за проханням («дуже мало руди») — і розмір покладів,
   і шанс на регіон зросли приблизно вдвічі відносно попередніх значень. */
export const ORE_VEINS: Partial<Record<BlockId, VeinSpec>> = {
  coal:     { min: 7, max: 13, chance: (r) => 0.85 * ramp(r, 1, 3)  + 0.30 * ramp(r, 3, 150) },
  redstone: { min: 5, max: 8,  chance: (r) => 0.50 * ramp(r, 2, 6)  + 0.20 * ramp(r, 6, 200) },
  iron:     { min: 6, max: 9,  chance: (r) => 0.65 * ramp(r, 2, 6)  + 0.24 * ramp(r, 6, 200) },
  lapis:    { min: 3, max: 6,  chance: (r) => 0.32 * ramp(r, 5, 14) + 0.15 * ramp(r, 14, 250) },
  gold:     { min: 4, max: 6,  chance: (r) => 0.42 * ramp(r, 3, 10) + 0.18 * ramp(r, 10, 250) },
  // алмаз глибший: з'являється з ~9 ряду (було з 4), виходить на плато до ~22
  diamond:  { min: 3, max: 5,  chance: (r) => 0.25 * ramp(r, 9, 22) + 0.15 * ramp(r, 22, 320) },
  emerald:  { min: 2, max: 4,  chance: (r) => 0.12 * ramp(r, 16, 50) + 0.09 * ramp(r, 50, 350) },
};

/* Таблиця прокруту рулетки: «пусто» + усі кірки.
   null у полі tier = «пусто». */
export interface ReelSlot { tier: Tier | null; weight: number }

export function reelTable(): ReelSlot[] {
  const table: ReelSlot[] = [{ tier: null, weight: CONFIG.nothingWeight }];
  for (const t of TIERS) table.push({ tier: t, weight: t.weight });
  return table;
}

/* Тільки кірки, без «пусто» — для pity-прокруту (гарантована кірка).
   Розклад тірів той самий, що на звичайній рулетці. */
export function tierOnlyTable(): ReelSlot[] {
  return TIERS.map((t): ReelSlot => ({ tier: t, weight: t.weight }));
}

/* Таблиця множників БОНУСНОЇ шахти: ті самі номінали, але великі ікси
   важчі за CONFIG.buy.tailBoost. Дрібні не чіпаються й просто втрачають
   частку.

   Будується один раз на модуль. На детермінізм підміна не впливає:
   pickWeighted() витрачає рівно один виклик rnd() незалежно від вмісту
   таблиці, а про bonus клієнт і сервер знають однаково. */
export const MULT_TABLE_BONUS: readonly { m: number; weight: number }[] =
  CONFIG.mult.table.map((e) => ({
    m: e.m,
    weight: e.weight * (CONFIG.buy.tailBoost[e.m] ?? 1),
  }));
