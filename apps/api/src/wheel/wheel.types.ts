/* ============================================================
   КОЛЕСО ЩОДЕННОГО БОНУСУ — правила призів.

   Окремо від сервісу, бо це ЄДИНЕ місце, де описано, що саме може
   випасти. Клієнт малює сектори з цього ж списку (віддається по
   дроту), тож розійтись картинка з математикою не може.

   ЧОМУ ВАГИ, А НЕ ШАНСИ У ВІДСОТКАХ. Ваги складаються, і додати приз
   означає дописати рядок, не перераховуючи решту. Сума лишається
   довільною — сервіс ділить на неї сам.
   ============================================================ */

export type WheelPrizeId = 'cash10' | 'cash25' | 'cash50' | 'cash100' | 'spins5';

export interface WheelPrize {
  id: WheelPrizeId;
  /** скільки ₽ на баланс (0 — приз не грошовий) */
  rub: number;
  /** скільки безкоштовних прокрутів (0 — приз грошовий) */
  spins: number;
  /** як показуємо в секторі */
  label: string;
  weight: number;
}

/* Порядок у масиві = порядок секторів на колесі, за годинниковою від
   верху. Свідомо чергуємо дрібне з великим, щоб джекпот не стояв
   поруч із найчастішим сектором: інакше на кожному прокруті здавалось,
   що «майже влучив».

   Ваги: 10 ₽ — половина всіх прокрутів, 100 ₽ — один із двадцяти,
   п'ять фріспінів — один із двохсот. */
export const WHEEL_PRIZES: readonly WheelPrize[] = [
  { id: 'cash10', rub: 10, spins: 0, label: '10 ₽', weight: 100 },
  { id: 'cash50', rub: 50, spins: 0, label: '50 ₽', weight: 25 },
  { id: 'cash25', rub: 25, spins: 0, label: '25 ₽', weight: 60 },
  { id: 'spins5', rub: 0, spins: 5, label: '5 фриспинов', weight: 1 },
  { id: 'cash100', rub: 100, spins: 0, label: '100 ₽', weight: 10 },
  { id: 'cash25', rub: 25, spins: 0, label: '25 ₽', weight: 60 },
];

/* Перший прокрут у житті акаунта — ЗАВЖДИ цей приз, без жеребкування.
   Це вітальний подарунок, а не лотерея: нова людина має побачити
   відчутну суму, а не 10 ₽ і розчарування на першому ж екрані. */
export const WHEEL_FIRST_PRIZE: WheelPrizeId = 'cash100';

/* Наступний прокрут — через стільки після ПОПЕРЕДНЬОГО (не опівночі).
   Прив'язка до моменту прокруту, а не до календарної доби, прибирає
   гру з годинниковими поясами й «двічі поспіль о 23:59 і 00:01». */
export const WHEEL_COOLDOWN_MS = 24 * 60 * 60 * 1000;

/* Ставка, на якій грають подаровані колесом прокрути.

   ФІКСОВАНА НА СЕРВЕРІ, і це принципово. Якби фріспіни грались на
   поточній ставці гравця, вистачило б перед прокрутом виставити
   максимум — і подарунок у 5 прокрутів коштував би десять тисяч.
   Мінімальна ставка теж не годиться: 5 прокрутів по 10 ₽ дешевші за
   грошовий приз у 100 ₽, і найрідкісніший сектор колеса був би
   найгіршим. 100 ₽ ставить фріспіни вище за будь-який грошовий
   сектор, лишаючи їх при цьому передбачуваними за вартістю. */
export const WHEEL_FREE_BET = 100;
